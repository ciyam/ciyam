if [ $# -lt 1 ]; then
 echo Usage: init [model name] [[app name]]
else
 export CIYAM_MODEL=$1
 export CIYAM_STORAGE=$1
 if [ $# -gt 1 ]; then
  export CIYAM_STORAGE=$2
 fi
 rm -f $1.modules.lst $1.log $1.dat $1.idx $1.hdr >/dev/null
 ./cat_client -quiet -no_prompt < init.cin
fi
